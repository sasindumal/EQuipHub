# Production Profile
spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
  devtools:
    restart:
      enabled: false

logging:
  level:
    root: WARN
    com.equiphub: INFO
  file:
    name: /var/log/equiphub/api.log
